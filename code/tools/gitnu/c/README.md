# gitnv

Inspired by [holygeek/git-number](https://github.com/holygeek/git-number).

1. It provides `git nu status` as a custom, opnionated, `git status` output.
2. Writes the cache file to the git directory.
3. Allows users to use indices as pathspecs.
